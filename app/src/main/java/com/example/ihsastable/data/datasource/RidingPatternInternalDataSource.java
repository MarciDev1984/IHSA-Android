package com.example.ihsastable.data.datasource;

public class RidingPatternInternalDataSource {
    // should act as the initialization of the data source
    // gives the data source to the repository to edit
}
